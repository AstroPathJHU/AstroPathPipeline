#imports
from ...shared.sample import WorkflowSample

class FlatfieldSample(WorkflowSample) :
    """
    Main class to collect and work with meanimage and flatfield information from a single sample
    """
